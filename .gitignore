# Per-user — copy from the matching .example.
.env
.gitconfig
skills.conf

# Runtime state, managed by the tau CLI.
workspaces.conf
workspace/*
pi-config/*

# Personal agent scripts — see scripts/README.md.
scripts/*
!scripts/README.md
